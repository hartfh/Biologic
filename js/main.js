require(['classes/Layer', 'classes/Entity', 'classes/Matrix/Circle'], function(Layer, Entity, Circle) {
	var testLayer = new Layer({name: 'primary-layer'});

	var args = {width: 2, height: 2};
	var test = new Entity(args);
});
